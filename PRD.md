<!-- vibe-memory: status=accepted; verified_at=待确认; verified_commit=待确认; owner=product -->
# LoopzToken 产品需求文档（PRD）

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 产品名称 | LoopzToken |
| 产品类型 | 多模型 API 聚合、销售与用量管理平台 |
| 目标市场 | 中国大陆个人开发者、独立开发者与技术团队 |
| 商业模式 | 用户预充值人民币余额，按实际模型用量扣费 |
| 文档状态 | 分阶段生产版本规划稿 |
| 更新日期 | 2026-08-25 |

## 2. 产品概述

LoopzToken 将多个已合法授权的模型 API 渠道整合为统一入口。用户注册、充值并创建 LoopzToken API Key 后，可以通过 OpenAI、Anthropic、Gemini 等兼容协议调用平台已上架的模型，并在控制台查看余额、逐请求用量、价格与账单。

平台不绑定单一上游。每个公开模型可以配置一个或多个上游部署，系统依据协议兼容性、健康度、并发额度、成本和运营策略选择合适渠道。用户始终使用 LoopzToken 的 Base URL 与 API Key，上游变化不应迫使用户改动接入配置。

## 3. 产品目标

### 3.1 用户目标

- 一个账户、一笔余额、一组 API Key 调用多个模型。
- 使用主流 SDK 和 Coding Agent 时只修改 Base URL 与 API Key。
- 清楚知道每次请求使用了什么模型、消耗多少 Token、扣了多少钱。
- 可以限制单个 API Key 的模型、额度、IP 和有效期。
- 上游渠道变化时保持稳定的公开模型名和接入方式。

### 3.2 商业目标

- 建立可追溯的充值、消费、退款与人工调整账本。
- 分模型、渠道和用户核算收入、上游成本及毛利。
- 支持微信支付、支付宝充值与服务端自动到账。
- 支持多渠道接入、灰度、停用和故障切换。
- 在不改变用户账户体系的前提下持续增加新模型和供应商。

### 3.3 技术目标

- 首发容量基线：3,000 条并发 SSE 长连接、100 RPS 新请求峰值。
- 请求体上限基线：32 MB；单次请求最长 600 秒。
- 平台月度可用性目标：99.9%（不包含上游模型自身不可用时间的责任边界，需在 SLA 中明确）。
- LoopzToken 网关附加延迟目标：P95 小于 30 ms，不包含上游推理耗时。
- 不允许重复充值、重复结算或用户余额变为负数。

以上容量是首版设计基线，正式上线规格以压测与云资源预算确认结果为准。

### 3.4 建设策略

产品按“首期生产闭环、容量触发演进”建设。首期必须达到正式收费、准确结算和 3,000 条并发 SSE 的验收标准，但不把消息队列、ClickHouse、Kubernetes 等基础设施作为上线前置条件。系统通过 PostgreSQL Outbox、稳定事件契约和无状态服务为后续扩展预留接口。

这属于同一套成熟架构的分阶段部署，不是降低账务、安全或容量要求的临时版本。

## 4. 非目标

首发版本不包含：

- 自研基础大模型。
- 向用户展示或出售上游原始 API Key。
- 未经授权的账号池、订阅账号转换或灰色渠道。
- 自动将用户指定模型静默替换为低规格模型。
- 移动 App 和桌面客户端。
- 图像、音频、视频生成接口。
- 企业组织、部门、成员及复杂审批。
- 月度自动续费订阅和邀请返佣。
- 面向用户的 Prompt 托管或聊天历史功能。

## 5. 用户角色

### 5.1 个人开发者

关注快速接入、余额透明、价格明确和 Coding Agent 兼容。

### 5.2 独立开发者或小团队负责人

关注多个 API Key、预算控制、用量统计、稳定性和故障定位。

### 5.3 平台运营人员

负责用户、订单、退款、模型、价格、渠道、毛利、风控与公告。

### 5.4 平台技术管理员

负责上游接入、协议映射、路由规则、限流、告警、密钥轮换和故障处置。

## 6. 核心用户旅程

### 6.1 注册到首次调用

1. 用户通过邮箱或手机号注册并完成验证。
2. 用户阅读并同意用户协议、隐私政策和服务规则。
3. 用户进入模型广场查看价格与支持协议。
4. 用户通过微信或支付宝充值。
5. 支付平台异步通知 LoopzToken，订单到账。
6. 用户创建 API Key，并选择允许的模型与额度限制。
7. 用户复制对应工具的配置或 API 示例。
8. 用户发起首次模型请求。
9. 控制台展示请求状态、Token 分类和人民币费用。

### 6.2 日常使用

1. 用户使用固定 Base URL 与 API Key 发起请求。
2. 平台验证 Key、余额、模型权限和限流。
3. 平台预冻结最大可能费用并选择上游部署。
4. 平台流式转发上游响应。
5. 请求完成后按实际用量结算并释放多余冻结金额。
6. 用户在用量页面查看逐请求明细。

### 6.3 异常请求

1. 上游在首个响应数据前失败时，平台根据规则尝试同模型备用部署。
2. 已开始向用户输出后，不拼接其他上游响应。
3. 用户中途断开时，平台停止下游输出并核对已产生的上游费用。
4. 暂时无法确定用量时，请求进入“待结算”，冻结金额在最终核对后处理。

### 6.4 退款

1. 用户提交未消费余额退款申请。
2. 平台检查支付来源、剩余可退金额、赠送金额和风险状态。
3. 运营人员审核或系统按规则自动审核。
4. 原路发起退款。
5. 退款结果通过支付平台通知和主动查询双重确认。
6. 钱包与退款流水同步更新。

## 7. 产品信息架构

### 7.1 官网

- 首页
- 模型与价格
- 支持的工具
- API 文档
- 服务状态
- 常见问题
- 用户协议
- 隐私政策
- 退款规则
- 联系与投诉

### 7.2 用户控制台

- 概览
- API Key
- 模型与价格
- 用量明细
- 余额与充值
- 订单与退款
- 工具配置
- 账号与安全
- 服务工单

### 7.3 运营后台

- 用户管理
- 模型管理
- 价格版本
- Provider 与渠道管理
- 路由策略
- 订单与支付
- 钱包流水
- 退款管理
- 请求和用量
- 毛利与对账
- 风控事件
- 公告与工单
- 审计日志
- 系统配置

## 8. 功能需求

优先级定义：

- P0：正式上线必需。
- P1：上线后近期必需。
- P2：后续增强。

### 8.1 账号与安全

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| AUTH-001 | P0 | 支持邮箱或手机号注册、登录和验证码校验。 |
| AUTH-002 | P0 | 支持密码找回、会话退出和所有设备退出。 |
| AUTH-003 | P0 | 用户注册时记录协议版本和同意时间。 |
| AUTH-004 | P0 | 管理员必须启用双因素认证。 |
| AUTH-005 | P0 | 登录、注册、验证码和找回密码接口实施限流。 |
| AUTH-006 | P1 | 支持 Passkey 或第三方 OAuth 登录。 |

### 8.2 API Key

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| KEY-001 | P0 | 用户可创建、命名、禁用和删除 API Key。 |
| KEY-002 | P0 | 完整 Key 只在创建时展示一次。 |
| KEY-003 | P0 | 支持设置过期时间、每日金额上限和模型白名单。 |
| KEY-004 | P0 | 支持设置每分钟请求量、Token 量和并发限制。 |
| KEY-005 | P1 | 支持 IP 白名单。 |
| KEY-006 | P1 | 支持 Key 泄露检测和异常调用提醒。 |

### 8.3 模型与协议

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| MODEL-001 | P0 | 提供 `GET /v1/models`。 |
| MODEL-002 | P0 | 提供 OpenAI Chat Completions 兼容接口。 |
| MODEL-003 | P0 | 提供 OpenAI Responses 兼容接口。 |
| MODEL-004 | P0 | 提供 Anthropic Messages 原生兼容接口。 |
| MODEL-005 | P0 | 支持 SSE 流式和非流式响应。 |
| MODEL-006 | P0 | 模型必须配置允许协议，不兼容时在转发前返回明确错误。 |
| MODEL-007 | P0 | 不允许静默跨模型降级。 |
| MODEL-008 | P1 | 提供 Gemini 原生兼容接口。 |
| MODEL-009 | P2 | 增加图像、音频、视频等多模态接口。 |

### 8.4 多渠道与路由

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| ROUTE-001 | P0 | 支持创建任意数量 Provider 和上游部署。 |
| ROUTE-002 | P0 | 支持 OpenAI 兼容、Anthropic 原生、Gemini 原生和自定义 HTTP Adapter。 |
| ROUTE-003 | P0 | 同一公开模型可配置多个等价部署。 |
| ROUTE-004 | P0 | 路由依据模型、协议、健康度、优先级、成本、地区和剩余额度。 |
| ROUTE-005 | P0 | 仅在首个响应数据前允许安全故障切换。 |
| ROUTE-006 | P0 | 每个请求记录完整路由链和上游 Request ID。 |
| ROUTE-007 | P0 | 支持渠道熔断、半开探测和手工摘除。 |
| ROUTE-008 | P1 | 支持按用户组、Key 或价格方案选择 Provider。 |
| ROUTE-009 | P1 | New API 可作为一个可选内部 Provider 接入，但不能成为用户账务权威。 |

### 8.5 钱包与计费

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| BILL-001 | P0 | 人民币钱包采用整数微元存储，不使用浮点数。 |
| BILL-002 | P0 | 每次余额变化必须生成不可变流水。 |
| BILL-003 | P0 | 请求开始前预冻结余额，请求完成后实际结算。 |
| BILL-004 | P0 | 支持输入、输出、缓存写入、缓存读取、推理等 Token 分类。 |
| BILL-005 | P0 | 每个请求绑定不可修改的价格版本。 |
| BILL-006 | P0 | 结算、释放、充值和退款必须幂等。 |
| BILL-007 | P0 | 不允许负余额和重复扣款。 |
| BILL-008 | P0 | 支持本地用量与上游账单对账。 |
| BILL-009 | P0 | 请求必须具有可追踪的计费状态机，异常进入待结算并可自动补偿或人工复核。 |
| BILL-010 | P0 | 结算保存 Usage 来源、解析器版本和是否估算，不允许无证据静默扣费。 |
| BILL-011 | P1 | 支持不同用户组和企业合同价格。 |

### 8.6 充值、支付与退款

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| PAY-001 | P0 | 支持微信支付官方商户接口。 |
| PAY-002 | P0 | 支持支付宝官方商户接口。 |
| PAY-003 | P0 | 支付成功只以服务端异步通知或主动查单结果为准。 |
| PAY-004 | P0 | 支付通知必须验签、校验商户、金额和订单状态。 |
| PAY-005 | P0 | 支持支付成功但余额未到账的自动补偿。 |
| PAY-006 | P0 | 支持未消费购买余额原路退款及退款流水。 |
| PAY-007 | P0 | 赠送余额与购买余额分开记录，并明确退款规则。 |
| PAY-008 | P1 | 支持优惠券和运营赠送。 |

### 8.7 用量与账单

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| USAGE-001 | P0 | 用户可以按时间、模型、Key 和状态查询请求。 |
| USAGE-002 | P0 | 明细展示 Token 分类、价格版本和人民币费用。 |
| USAGE-003 | P0 | 不向用户暴露上游密钥和内部敏感渠道信息。 |
| USAGE-004 | P0 | 提供每日和每月用量趋势。 |
| USAGE-005 | P1 | 提供缓存命中率、首 Token 延迟和错误率。 |
| USAGE-006 | P1 | 支持 CSV 账单导出。 |

### 8.8 运营与风控

| 编号 | 优先级 | 需求 |
| --- | --- | --- |
| OPS-001 | P0 | 管理员可以启用、禁用用户和 API Key。 |
| OPS-002 | P0 | 管理员可以上下架模型和渠道。 |
| OPS-003 | P0 | 价格调整必须新建价格版本，不覆盖历史价格。 |
| OPS-004 | P0 | 人工余额调整需要原因、操作人和审计记录。 |
| OPS-005 | P0 | 识别高频调用、Key 共享、暴力尝试和异常消费。 |
| OPS-006 | P0 | 提供上游余额不足、毛利异常、成功率下降告警。 |
| OPS-007 | P1 | 支持服务公告和用户工单。 |

## 9. 定价规则

每个模型至少支持以下价格：

```text
输入 Token 单价
输出 Token 单价
缓存写入 Token 单价
缓存读取 Token 单价
推理 Token 单价
特殊服务等级附加倍率
```

用户费用：

```text
输入费用
+ 输出费用
+ 缓存写入费用
+ 缓存读取费用
+ 推理费用
+ 服务等级附加费用
```

上游成本和用户售价必须分开管理。用户售价在价格版本有效期内保持确定，上游动态价格变化只影响成本和毛利监控，不应改变已经开始的请求价格。

## 10. 隐私与内容处理

- 默认不保存完整 Prompt 和模型回复。
- 保存请求元数据、Token 用量、错误类型、延迟、路由和账务信息。
- 调试内容日志只能由用户主动开启，并设置明确保留时间。
- Authorization、Cookie、API Key、密码和支付敏感字段必须脱敏。
- 在隐私政策中说明模型请求将发送给所选上游服务商处理。
- 上游合同需明确数据用途、保留、删除、保密和安全事件通知责任。
- 涉及境外处理时，上线前完成适用的数据跨境合规评估。

## 11. 非功能需求

### 11.1 性能

- 3,000 条并发 SSE 连接基线。
- 100 RPS 新请求峰值基线。
- 32 MB 请求体上限基线。
- 最大请求持续时间 600 秒。
- Gateway 附加延迟 P95 小于 30 ms。
- 流式转发不能缓存完整模型响应。

### 11.2 可用性

- Gateway、Billing、Control API 均支持多实例。
- 任一 Gateway 实例故障不影响存量钱包数据。
- Provider 故障不能阻塞其他 Provider。
- 关键服务必须有健康检查、熔断和容量告警。

### 11.3 一致性

- 钱包余额、冻结和流水在同一数据库事务内更新。
- 支付通知、结算与退款均使用唯一幂等键。
- 分析系统不能反向修改权威账本。
- 对账差异必须可定位到请求和 Provider Request ID。

### 11.4 安全

- 用户密码使用 Argon2id。
- API Key 只保存 HMAC 哈希。
- 上游密钥使用密钥管理服务或加密配置保存。
- 管理端使用最小权限和双因素认证。
- 防止 SSRF、越权、重放、请求走私和日志泄密。

## 12. 产品指标

### 12.1 业务指标

- 注册到首次成功调用转化率。
- 充值用户比例。
- 首次充值金额和复购率。
- 日活跃 API Key 数量。
- 用户收入、上游成本和毛利率。
- 退款率和支付到账异常率。

### 12.2 技术指标

- 请求成功率。
- P50、P95、P99 首 Token 延迟。
- SSE 中断率。
- Provider 成功率和熔断次数。
- 本地用量与上游账单差异率。
- 负余额、重复充值、重复扣费数量，目标均为 0。

## 13. 上线验收标准

### 13.1 功能验收

- 用户可完成注册、充值、创建 Key 和模型调用闭环。
- OpenAI Chat、Responses 和 Anthropic Messages 均通过真实客户端测试。
- 流式、非流式、Tool Calling、Reasoning 和缓存 Token 按支持范围完成验证。
- 用户可查询逐请求账单。
- 运营人员可处理退款、禁用用户和上下架模型。

### 13.2 账务验收

- 支付通知重复 100 次只到账一次。
- 结算接口重复调用不重复扣款。
- 并发请求无法造成负余额。
- 用户中途断开后可以完成最终结算或进入可追踪的待结算状态。
- 待结算请求默认 5 分钟内完成自动处理，超过 24 小时进入人工复核并告警。
- 每条费用可追溯到价格版本、Usage 来源、上游尝试和 Provider Request ID。
- 日终账本、支付平台和上游成本对账可执行。

### 13.3 容量验收

- 在模拟上游下完成 3,000 条并发 SSE 连接压测。
- 在目标请求体和输出速率下内存达到稳定平台，不持续增长。
- 单节点退出时存量连接按预期中断，新增连接自动转移到健康节点。
- 数据库和 Redis 故障演练符合降级策略。

### 13.4 安全与运维验收

- 上游密钥、支付密钥和用户 Key 不出现在日志与前端资源中。
- 完成数据库备份恢复演练。
- 完成管理员权限和审计检查。
- 告警能够触达值守人员。
- 法律页面、备案、支付和上游授权材料满足上线要求。

## 14. 发布阶段

### 阶段 A：核心链路可验证

- 多协议 Gateway。
- Provider Adapter。
- API Key、钱包、冻结和结算。
- 至少两个测试 Provider。
- 模拟支付闭环。
- PostgreSQL Outbox、待结算补偿和计费状态机。

退出条件：协议兼容测试和账务并发测试通过，Mock Payment 可完整跑通充值与退款。

### 阶段 B：首期生产闭环

- 微信、支付宝正式接入。
- 用户控制台和运营后台。
- 退款、风控、审计和日终对账。
- 使用 PostgreSQL 汇总表提供首期用量、成本和毛利统计。

退出条件：商户能力已开通，支付验签、重复通知、到账补偿和日终对账测试通过。

### 阶段 C：容量与灰度

- 使用托管容器或云主机多实例部署首期生产拓扑。
- 高并发、故障和账务压测。
- 小规模真实用户灰度。
- 根据压测结果调整实例和限额。

退出条件：3,000 条并发 SSE、目标大请求体、节点故障和数据库恢复验收通过。

### 阶段 D：正式开放

- 开放公众注册和充值。
- 持续监控 Provider 成本、毛利和稳定性。
- 根据真实指标逐步增加渠道和模型。

### 阶段 E：规模化演进

- Outbox 消费或事件扇出达到瓶颈后引入 Redpanda/Kafka。
- PostgreSQL 分析查询影响在线事务后引入 ClickHouse。
- 发布、服务规模和弹性需求达到阈值后迁移托管 Kubernetes。
- 业务与合规需要明确后建设第二地域和独立 Provider Egress。

阶段 E 不改变钱包和账本权威来源，也不作为阶段 D 开放公众充值的默认前置条件。

## 15. 待业务确认项

- 峰值目标最终按 3,000、5,000 还是 10,000 并发验收。
- 32 MB 请求体上限是否覆盖目标客户场景。
- 公开模型命名采用上游原名还是 LoopzToken 稳定别名。
- 首批 Provider、模型和销售价格。
- 微信支付、支付宝商户产品开通时间。
- 购买余额、赠送余额和退款的财务及税务规则。
